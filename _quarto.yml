project:
  type: website
  output-dir: docs

resources:
  - "exercises/*"

execute:
  freeze: auto  # re-render only when source changes

website:
  title: "Introduction to Bayesian Statistics"
  navbar:
    left:
      - href: index.qmd
        text: home
format:
  html:
    theme:
      - cosmo
      - brand
    css: styles.css
    toc: true
